box: kojiwakayama/nodejs-ruby-sass

build:
  steps:
    - npm-install
    - plasticine/bower-install
    - hgen/gulp

deploy:
  steps:
    - s3sync:
        key_id: $AWS_ACCESS_KEY_ID
        key_secret: $AWS_SECRET_ACCESS_KEY
        bucket_url: $AWS_BUCKET_URL
        source_dir: $SOURCE_DIR